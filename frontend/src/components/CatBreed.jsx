import React from "react";
import ReactStars from "react-stars";
import { Col, Card, ListGroup, ListGroupItem } from "react-bootstrap";

const CatBreed = ({ breedsId }) => {
  return (
    <div>
      <h1>Card de cat</h1>
    </div>
  );
};

export default CatBreed;
